<div class="row">
  <div class="col-md-12">
    <% @questions.each do |question| %>
      <h3 class="question-title"><%= link_to question.title, question_path(question) %></h3>
    <% end %>
  </div>
</div>
